<resources>

    <!-- Base application theme. -->
    <style name="Theme" parent="Theme.AppCompat.Light">
        <item name="android:windowBackground">@drawable/fondo</item>
        <item name="android:actionBarStyle">@color/ripple_material_light</item>
    </style>



</resources>
